# #iterate over the list of students returned from the index action in the controller
<div>
    <% @students.each do |student| %>
        <div><%= student.to_s %><div># you need to turn it into a string, since it will have been in an array, but we cannot accept an array, thus .to_s
    <%end%>
</div>#research more on div
